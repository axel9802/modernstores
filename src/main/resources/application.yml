spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbmodernstores?useSSL=false&serverTimezone=UTC
    username: root
    password: 
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate: 
       dialect: org.hibernate.dialect.MySQL8Dialect
       dialect.storage_engine: innodb
server:
  port: 8080